{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "id": 1,
      "title": "DNS Correlation (matched/enriched)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum by (tier, enriched) (rate(llm_slo_correlation_total[5m]))",
          "legendFormat": "{{tier}} enriched={{enriched}}"
        }
      ],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "id": 2,
      "title": "Agent Heartbeat",
      "type": "timeseries",
      "targets": [
        {
          "expr": "max(llm_slo_agent_heartbeat)",
          "legendFormat": "heartbeat"
        }
      ],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["llm", "ebpf", "correlation"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "title": "Kernel Correlation",
  "version": 1
}
