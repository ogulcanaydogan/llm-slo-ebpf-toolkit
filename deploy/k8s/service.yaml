apiVersion: v1
kind: Service
metadata:
  name: llm-slo-agent-metrics
  namespace: llm-slo-system
spec:
  selector:
    app: llm-slo-agent
  ports:
    - name: metrics
      port: 2112
      targetPort: metrics
      protocol: TCP
