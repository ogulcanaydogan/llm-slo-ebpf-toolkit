apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-slo-collector-config
  namespace: llm-slo-system
data:
  collector-flags: |
    --scenario mixed
    --count 0
    --interval-ms 1000
    --output stdout
